This directory contains the code for the GUI.

Current objective
  -> QT C++ based
  -> Convert bitset<512> to byte[64] and send down serial line to arduino
  -> Unsure of setup to choose cube patterns.
  -> Need a way of converting text to LED patterns
