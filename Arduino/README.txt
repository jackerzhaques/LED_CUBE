This code is ready to compile for ARDUINO UNO ONLY (ATMEGA328).

If you wish to use another board you will have to modify the ports used (and of course the pins) in order to compile and run.

Just download this to the arduino and you are set.
